import React from "react";

type Props = {
    videoId: string;
    author: string;
};

const CommentForm = ({ videoId, author }: Props) => {
    return (
  <div>
    hi there
  </div>
    );
};  

export default CommentForm;